{
    "name": "poolday",
    "private": true,
    "scripts": {
        "dev": "concurrently -n api,ui -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix ui\""
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}
