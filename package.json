{
  "name": "funpool",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,ui -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix ui\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
