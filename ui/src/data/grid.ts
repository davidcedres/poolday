export const CELL_W = 204;   // px per grid column
export const CELL_H = 140;   // px per grid row
export const GRID_HALF = 50; // grid goes col/row -50 … +49
export const RENDER_RADIUS = 22; // max grid-distance from origin to render a cell
